import React from 'react';
import './styles.css';

/**
 * Componente que se muestra cuando la app no encuentra la ruta a la que se esta intentando acceder.
 * @returns 
 */

const NotFound = () => {
  return (
    <h1>Page not found.</h1>
  )
}

export default NotFound