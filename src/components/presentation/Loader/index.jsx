import React from "react";
import "./styles.css";

/**
 * Componente que muestra la animación de cargando. El script que lo anima se encuentra en 'public/assets/animacionCargando.js'.
 * @returns 
 */

const Loader = () => {
  return <p id="Loader">Cargando... /</p>;
};

export default Loader;
